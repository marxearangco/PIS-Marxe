<%= f.fields_for :services do |g| %>
				<%= g.collection_check_boxes(:appointment, Product.all, :id, :name) do |b| %>
				<div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
					<%= b.check_box(class: "check_box") %>
					<%= b.object.name %>
				</div>
				<% end %>
				<% end %>